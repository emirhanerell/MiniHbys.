@model MiniHbys.Entity.Role

@{
    ViewBag.Title = "Edit Role";
    Layout = "_Layout";
}


@using (Html.BeginForm(actionName: "Edit", controllerName: "Role", routeValues: null, method: FormMethod.Post))
{
    @Html.HiddenFor(x => x.RoleID)
    <div class="content" style="padding: 20px">
        <div class="form-group">
            @Html.TextBoxFor(x => x.RoleName, htmlAttributes: new
            {
                @class = "form-control", placeholder =
                    "Role Name"
            })
        </div>
        <div class="form-group">
            @Html.TextBoxFor(x => x.RoleDescription, htmlAttributes: new { @class = "form-control", placeholder = "Role Description" })
        </div>
        <div class="form-group">
            @Html.CheckBoxFor(x => x.IsSuperAdmin, htmlAttributes: new { placeholder = "Is Super Admin" })
            <label>Is Super Admin</label>
        </div>
        <div class="form-group">
            @Html.CheckBoxFor(x => x.DepartmentReadAccess, htmlAttributes: new {  placeholder = "Department Read Access" })
            <label>Department Read Access</label>
        </div>
        <div class="form-group">
            @Html.CheckBoxFor(x => x.DepartmentWriteAccess, htmlAttributes: new { placeholder = "Department Write Access" })
            <label>Department Write Access</label>
        </div>
        <div class="form-group">
            @Html.CheckBoxFor(x => x.DoctorReadAccess, htmlAttributes: new { placeholder = "Doctor Read Access" })
            <label>Doctor Read Access</label>
        </div>
        <div class="form-group">
            @Html.CheckBoxFor(x => x.DoctorWriteAccess, htmlAttributes: new {  placeholder = "Doctor Write Access" })
            <label>Doctor Write Access</label>
        </div>
        <div class="form-group">
            @Html.CheckBoxFor(x => x.InspectionReadAccess, htmlAttributes: new { placeholder = "Inspection Read Access" })
            <label>Inspection Read Access</label>
        </div>
        <div class="form-group">
            @Html.CheckBoxFor(x => x.InspectionWriteAccess, htmlAttributes: new {  placeholder = "Inspection Write Access" })
            <label>Inspection Write Access</label>
        </div>

        <div class="form-group">
            @Html.CheckBoxFor(x => x.PatientReadAccess, htmlAttributes: new { placeholder = "Patient Read Access" })
            <label>Patient Read Access</label>
        </div>
        <div class="form-group">
            @Html.CheckBoxFor(x => x.PatientWriteAccess, htmlAttributes: new {placeholder = "Patient Write Access" })
            <label>Patient Write Access</label>
        </div>

        <div class="form-group">
            @Html.CheckBoxFor(x => x.MedicalOperationReadAccess, htmlAttributes: new { placeholder = "Medical Operation Read Access" })
            <label>Medical Operation Read Access</label>
        </div>
        <div class="form-group">
            @Html.CheckBoxFor(x => x.MedicalOperationWriteAccess, htmlAttributes: new
            {
                placeholder = "Medical Operation Write Access"
            })
            <label>Medical Operation Write Access</label>
        </div>

        <div class="form-group">
            @Html.CheckBoxFor(x => x.MedicineItemReadAccess, htmlAttributes: new {  placeholder = "MedicineItem Read Access" })
            <label>MedicineItem Read Access</label>
        </div>
        <div class="form-group">
            @Html.CheckBoxFor(x => x.MedicineItemWriteAccess, htmlAttributes: new
            {
                placeholder = "Medicine Item Write Access"
            })
            <label>Medicine Item Write Access</label>
        </div>
        <div style="float: right">
            <button type="submit" class="btn btn-success">Save</button>
        </div>
        @if (ViewBag.Message != null)
        {
            <div class="alert alert-danger alert-dismissible" style="width: 50%">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
                <h5><i class="icon fas fa-ban"></i> Alert!</h5>
                @ViewBag.Message
            </div>
        }
    </div>
}